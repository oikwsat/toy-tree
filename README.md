# toy-tree

ディレクトリ内をツリー表示するコマンド。WEB+DB PRESS Vol.117

# install

```
$ git clone
$ cd toy-tree
$ npm install --global
```

# Usage

    Usage
        $ toy-tree <directory>

    Example
        $ toy-tree
        $ toy-tree path/to/dir
